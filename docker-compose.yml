version: "3.9"

services:
  meanbot:
    build: .
    container_name: meanbot
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./mean_reversion.db:/app/mean_reversion.db
